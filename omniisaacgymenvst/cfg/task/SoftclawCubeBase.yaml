# See schema in factory_schema_config_base.py for descriptions of parameters.

defaults:
  - _self_
  - /factory_schema_config_base

sim:
  add_damping: True
  disable_contact_processing: False
    
env:
  env_spacing: 3.0
  franka_depth: 0.0
  table_height: 0.1 #2  # 0.3
  franka_friction: 1.0
  table_friction: 0.9 #0.3
  cube_max_scale: 1.5
  cube_min_density: 1500 #1500 # 1000 # 1800.0
  cube_max_density: 3000 #3000 # 1500 # 2300.0 
  cube_size: 0.025
  spawn_obstacle: False


 ## 78s: increase density, decrease learning rate, increase friction, increased goal zone radius, decrease kickstart length, cube starts closer (cube_pos_initial_max_r to 0.45)
 # removed force penalty.